python -V
pip list
pip install django
-- *instalar las dependencias a partir del archivo requirements.txt 
pip install -r requirements.txt 

-1 crear entorno virtual (simula)
python -m venv env (nombre_entorno)

-2 activar le entorno
call nientorno\Scripts\activate -- (cmd)win
source nientorno/Scripts/activate (git bash) -- win

source nientorno/bin/activate --mac/linux

-3 instalar dependencias en el entorno
pip list
pip install django
pip list

-4 crear proyecto
eje: django-admin startproject nombre_proyecto

django-admin startproject portafolio

-- accedemos al proyecto
cd portafolio

-- creamos una aplicación dentro del proyecto
python manage.py startapp login

-- agregar app al setting
INSTALLED_APPS = [
        ...,
        'login'
]

-- crear en nuestra app el archivo urls.py

-- en urls.py principal importamos las rutas de nuestras apps.
from django.urls import include,path
urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('login.urls')),
]

-- configuramos nuestras rutas login->urls.py
from django.urls import path
from login import views

urlpatterns = [
    path('', views.home, name='home')
]

-- en views de nuestra app:
from django.shortcuts import render, HttpResponse

# Create your views here.
def home(request):
    return HttpResponse("""
                        <h1> Hola, soy Mijail </h1>
                        """)


-- DESPLEGAR APLICACION

python manage.py runserver

*************** 2 Migraciones ***********************

-- crear tablas de app por default
python manage.py migrate

-- crear super usuario
python manage.py createsuperuser
-- pass:Admin1234

python manage.py runserver
http://127.0.0.1:8000/admin

http://127.0.0.1:8000/contacto

*************** 3 ORM ***********************
1. crear el usuario en postgresql
2. crear una base da datos con el mismo nombre del usuario
3. instalamos en nuestro entorno pip install psycopg2-binary
4. volvemos a las migraciones y ejecutamos

select * from projectos;
*****
projectos = Projecto.object.all() 

5. crear un app proyectos
python manage.py startapp proyectos
las urls
6. crear el modelo
7. ejecutar la migraciones ->
python -m pip install Pillow
python manage.py makemigrations
python manage.py migrate

********************* CRUD ************************
1. registrar modelo en admin
from proyectos.models import Proyecto

# Register your models here.
admin.site.register(Proyecto)

2. crear archivo forms.py, en nuestra app
from django import forms
from .models import Proyecto

class ProyectoForm(forms.ModelForm): 
    class Meta:
        model: Proyecto
        fields= '__all__'

3. construir metodos en views